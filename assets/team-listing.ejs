```{=html}
<div class="list team">
    <% for (const post of items) { %>
    <div class="team-entry" <%= metadataAttrs(post) %>>

        <% if (post.image) { %>
        <div class="thumbnail">
            <img src="<%= post.image %>" alt="<%= post.title %>" title="<%= post.title %>" />
        </div>
        <% } %>

        <div class="body">
            <p class="title listing-title"><a href="<%- post.path %>"><%= post.title %></a></p>

            <p class="description listing-description"><%= post.description %></p>

            <% if (post.about.links) { %>
              <div class="about-links listing-links">
                <% for (const link of post.about.links) { %>
                  <a href="'<%= link.href %>'" class="about-link" rel target>
                    <i class="<%= link.icon %>"></i>
                    <span class="about-link-text"><%= link.text %></span>
                  </a>
                <% } %>
              </div>
            <% } %>
        </div>

    </div>
    <% } %>
</div>
```
